# CRM Система обработки заявок (Тестовое задание)

Полноценная мини-CRM система для сбора заявок с внешних сайтов через виджет и управления ими в админ-панели.

## 🚀 Технологический стек
- **Backend:** Laravel 12.x, PHP 8.4
- **Database:** MySQL
- **Frontend:** Tailwind CSS (CDN), AJAX (Fetch API)
- **Библиотеки:**
  - `spatie/laravel-medialibrary` — обработка вложений.
  - `spatie/laravel-permission` — управление доступом.

---

## ✨ Основной функционал

### 1. Клиентский виджет (`/widget`)
- Универсальная форма отправки заявок.
- **Асинхронность:** Отправка данных без перезагрузки страницы (AJAX).
- **Валидация:** Проверка полей на лету, включая формат телефона (E.164).
- **Файлы:** Возможность прикреплять документы или скриншоты к тикету.

### 2. Админ-панель (`/admin`)
- **Управление статусами:** Смена состояний заявки (Новая, В работе, Завершена).
- **Фильтрация:** Поиск по Email, номеру телефона, дате создания и статусу.
- **Статистика:** Аналитические блоки с количеством заявок за сутки, неделю и месяц.

### 3. Архитектура
- **Service Layer:** Бизнес-логика вынесена из контроллеров в отдельные сервисы.
- **FormRequests:** Изолированная логика валидации.
- **Eloquent Scopes:** Оптимизированные запросы для фильтрации и статистики.

---

## 🛠 Установка и запуск

### 1. Подготовка окружения
```bash
# Клонирование
git clone [https://github.com/Artyr2828/crm_project.git](https://github.com/Artyr2828/crm_project.git)
cd crm_project

# Установка зависимостей
composer install

# Настройка .env
cp .env.example .env
php artisan key:generate

Откройте файл .env и укажите данные вашего MySQL:
```bash
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_db_name
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

```bash
# Предварительно создайте пустую БД в MySQL
php artisan migrate --seed
php artisan storage:link
